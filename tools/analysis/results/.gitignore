# Keep the results directory structure but ignore the actual result files
# This allows the results folder to exist in git while keeping generated files out

# Ignore all files in results directory
*

# But keep the README
!README.md

# Keep this .gitignore file
!.gitignore
